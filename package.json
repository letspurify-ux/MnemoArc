{
  "name": "mnemoarc",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build -w frontend && cargo build --release",
    "start": "cargo run --release -- web",
    "test": "npm run test:scripts && npm test -w frontend && cargo test",
    "test:scripts": "node --test scripts/services.test.mjs",
    "test:ui": "npm run build -w frontend && cargo build && npm run test:ui -w frontend"
  },
  "devDependencies": {
    "prettier": "^3.9.8"
  }
}
